import 'package:first_app/widgets/navigation_drawer/drawer_item';
import 'package:flutter/material.dart';
class NavigationDrawerr extends StatelessWidget {
  const NavigationDrawerr({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 300,
      decoration: const BoxDecoration(
        color: Colors.white,
        boxShadow:[ BoxShadow(color: Colors.black, blurRadius: 16),
        ]
      ),
      child: const Column(
        children: [
          NavigationDrawerr(),
          DrawerItem(title: 'Episodes', icon: Icons.videocam),
          DrawerItem(title: 'About', icon: Icons.help)
        ],
      ),
    );
  }
}

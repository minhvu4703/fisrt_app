import 'package:flutter/material.dart';
class EspisodesView extends StatelessWidget {
  const EspisodesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Episodes View'),);
  }
}
